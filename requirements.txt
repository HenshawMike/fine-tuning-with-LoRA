# requirements.txt

# Core libraries (CPU-only PyTorch)
torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu


# Hugging Face ecosystem
transformers>=4.40.0
datasets>=2.14.0
peft>=0.11.0
trl>=0.8.0
accelerate>=0.30.0
huggingface-hub>=0.23.0

# Optional but recommended for better experience
tokenizers>=0.19.0
sentencepiece>=0.2.0   # Required by Gemma models
protobuf>=4.25.0        # Sometimes needed for Gemma tokenizer

# For faster downloads (optional)
hf-transfer>=0.1.0